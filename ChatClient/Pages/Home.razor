@page "/"
@using ChatClient.Models
@using MudBlazor
@inject HttpClient Http

<MudContainer MaxWidth="MaxWidth.Medium" Class="mt-4">
    <MudText Typo="Typo.h5">Chat</MudText>

    <MudTextField @bind-Value="newMessage" Label="Enter message" Variant="Variant.Filled" />
    <MudButton OnClick="SendMessage" Color="Color.Primary" Class="mt-2">Send</MudButton>

    <MudDivider Class="my-2" />

    <MudList T="ChatMessage" Dense="true">
        @foreach (var msg in messages)
        {
            <MudListItem T="ChatMessage" Value="msg">
                @if (editMessageId == msg.Id)
                {
                    <MudTextField @bind-Value="editMessageText" Variant="Variant.Filled" />
                    <MudButton OnClick="@(() => SaveEdit(msg.Id))" Color="Color.Success" Size="Size.Small">Save</MudButton>
                    <MudButton OnClick="CancelEdit" Color="Color.Secondary" Size="Size.Small">Cancel</MudButton>
                }
                else
                {
                    @msg.Text
                    <MudButton StartIcon="@Icons.Material.Filled.Edit"
                               OnClick="@(() => StartEdit(msg))"
                               Color="Color.Primary"
                               Size="Size.Small"
                               Class="ml-2">
                        Edit
                    </MudButton>
                }
            </MudListItem>
        }
    </MudList>
</MudContainer>

@code {
    private string newMessage = "";
    private List<ChatMessage> messages = new();

    private Guid? editMessageId = null;
    private string editMessageText = "";

    protected override async Task OnInitializedAsync()
    {
        await LoadMessages();
    }

    private async Task LoadMessages()
    {
        messages = await Http.GetFromJsonAsync<List<ChatMessage>>("api/Messages") ?? new List<ChatMessage>();
    }

    private async Task SendMessage()
    {
        if (string.IsNullOrWhiteSpace(newMessage))
        {
            return;
        }
        var messageId = Guid.NewGuid();
        var sendMessageRequest = new SendMessageRequest()
        {
            Text = newMessage,
        };
        await Http.PutAsJsonAsync($"api/Messages/{messageId}", sendMessageRequest);
        newMessage = "";
        await LoadMessages();
    }

    private void StartEdit(ChatMessage message)
    {
        editMessageId = message.Id;
        editMessageText = message.Text;
    }

    private async Task SaveEdit(Guid messageId)
    {
        if (string.IsNullOrWhiteSpace(editMessageText))
        {
            return;
        }

        var sendMessageRequest = new SendMessageRequest()
        {
            Text = editMessageText,
        };
        await Http.PutAsJsonAsync($"api/Messages/{messageId}", sendMessageRequest);

        editMessageId = null;
        editMessageText = "";
        await LoadMessages();
    }

    private void CancelEdit()
    {
        editMessageId = null;
        editMessageText = "";
    }
}
